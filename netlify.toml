[build]
command = "curl -fsSL https://flutter.dev/docs/get-started/install/linux | bash && export PATH=\\$PATH:/opt/flutter/bin && flutter pub get && flutter build web"
publish = "build/web"

[context.production.environment]
FLUTTER_VERSION = "stable"
